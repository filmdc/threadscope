{
  "name": "threadscope",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "clean": "turbo clean",
    "db:generate": "pnpm --filter @threadscope/api db:generate",
    "db:push": "pnpm --filter @threadscope/api db:push",
    "db:migrate": "pnpm --filter @threadscope/api db:migrate",
    "db:studio": "pnpm --filter @threadscope/api db:studio"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
